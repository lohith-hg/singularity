# Copy this file to `.env` (gitignored) and set your NASA API key.
# Get a free key at https://api.nasa.gov/. If unset, the app falls back to
# NASA's public, rate-limited DEMO_KEY.
#
# Build/run with:  flutter run --dart-define-from-file=.env
NASA_API_KEY=
